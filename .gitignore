# The GitHub repo is PUBLIC. Only the website files below are ever uploaded.
# Everything else in this folder (Word CV, backups, session records, notes,
# CLAUDE.md) stays on this computer only.

# Ignore everything...
/*

# ...except the website itself
!/index.html
!/styles.css
!/photo.jpeg
!/CNAME
!/.nojekyll
!/.gitignore
